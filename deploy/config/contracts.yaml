contract_id: research-v1
goal: Research using web search; email requires approval
max_steps: 100
approvers: [alice]
agents:
  researcher:
    capabilities:
      web.search: allow
      email.send: escalate
---
contract_id: admin-v1
goal: Database maintenance
max_steps: 100
agents:
  db-admin:
    capabilities:
      database.write: allow
