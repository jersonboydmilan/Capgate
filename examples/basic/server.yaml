# harness token keygen --keyring examples/basic/keys.json
# harness serve examples/basic/server.yaml
# harness token issue --keyring examples/basic/keys.json --sub researcher --role agent --ttl 15m
contracts: [contract.yaml]
listen: {host: 127.0.0.1, port: 8700}
audit: audit.jsonl
state: state.db
identity:
  keyring_file: keys.json
  max_ttl_seconds: 3600
tools:
  web.search: {type: echo}
  notes.write: {type: echo}
